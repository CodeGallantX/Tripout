// import faqs from '../data/faqs.json';

const Faq = () => {
    return (
        <div>
            {/* <div>
                {faqs.map((faq, index => (
                    <div key={index}>
                        <p>{faq.question}</p>
                        <span>{faq.answer}</span>
                    </div>
                ))}
            </div> */}
        </div>
    )
}

export default Faq
